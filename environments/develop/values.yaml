

appName: worker

image:
  pullPolicy: IfNotPresent
  tag: latest
  repository: marcoscianci/shipay-worker
  command:

cronjob:
  enabled: true
  schedule: '0/1 * * *'
  command: ['/bin/bash', '-c']
  args:
    - echo "Start Command"
    - faust -A app worker -l info
    - echo "Complete Command"

environments:S